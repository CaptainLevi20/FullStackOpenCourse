{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../part_2/phonebook/ && npm run build && cp -r build ../../part_3/build",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.1.5",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "@stylistic/eslint-plugin": "^5.7.1",
    "eslint": "^9.39.2",
    "globals": "^17.1.0",
    "nodemon": "^3.1.11"
  }
}
